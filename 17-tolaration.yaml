apiVersion: v1
kind: Pod
metadata:
  namespace: roboshop
  name: toleration-pod
spec:
  containers:
  - name: nginx
    image: nginx

  tolerations:
  - key: "env"
    operator: "Equal"
    value: "dev"
    effect: "NoSchedule"