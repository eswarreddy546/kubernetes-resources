apiVersion: v1
Kind: service
metadata:
 name: service

spec:
 selector:
  name: roboshop
  project: frontend
  environment: dev

 ports:
  protocol: tcp
  port: 80 # service port
  targetport: 80 #container poart