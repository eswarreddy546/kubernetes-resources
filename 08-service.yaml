apiVersion: v1
kind: Service
metadata:
  name: service

spec:
  selector:
    name: roboshop
    project: frontend
    environment: dev

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80