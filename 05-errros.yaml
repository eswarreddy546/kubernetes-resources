apiVersion: v1
Kind : Pod
metadata:
  name: errors
  labels:
    namespace: roboshop
    name: labels-erros
    component: error
    project: dev
    environment: dev

spec:
  containers:
   - name: nginx
     image: nginx

