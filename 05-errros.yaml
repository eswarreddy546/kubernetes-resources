apiVersion: v1
Kind : Pod
metadata:
  name: errors
  labels:
    name: labels-erros
    component: error
    project: dev
    environment: dev

spec:
  containers:
   - name: nginxx
     image: nginxx

